# Python 3.10+ 권장. 실행 방법과 재현 범위: docs/reproduction.md
# GPU 환경에 맞는 PyTorch를 먼저 설치한 뒤 이 파일을 설치한다.

# RoBERTa/SCRN 학습, StyleBART SFT/DPOP, 주 평가
# 기존 학습 코드는 Transformers 4.x API를 사용한다.
transformers>=4.40,<5
torch>=2.0
numpy>=1.24

# 주 평가의 문장 분할·반복 진단에 필요
kiwipiepy>=0.20

# t-SNE 및 결과 그래프 재생성
matplotlib>=3.7
scikit-learn>=1.3

# 선택: scripts/metrics_quality.py의 별도 BERTScore 분석
# 주 결과의 ko-sroberta cosine과는 다른 지표다.
bert-score>=0.3.13

# 외부 도구: inverse rewriting/baseline용 llama-server (별도 설치·실행)
# Adversarial alignment/CopyKiller 도구: adversarial/requirements.txt 참조
